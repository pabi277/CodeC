# CodeC Website Phase W4.2 — Verification gate (locks the chapter set)

**Status:** 📋 **PLANNED** · **Cost:** `[static]` (docs-only deliverable) ·
**Effort:** M
· **Depends on:** nothing (README + repo config + BETA + RELEASE_NOTES + 38–43 docs) — **but runs first in W4**
· **Deliverable:** a **Verified Facts Table v2.2** committed in
  `web_docs/chat-web4/` + the locked 17-chapter set (closes **O6**)

> **v2.2 update (2026-09-12):** gate must re-verify new facts from Phases 21–43 — universal APK 6.6 MB signed SHA256, per-ABI splits reverted reason, Auto engine only picker deleted Termux card deleted fallback automatic four steps appear in Output Panel, >_ mark icon + ic_stat_codec + app_mark + sharp deterministic md5 + check_icon_assets CI, official file icons Seti MIT, typing feel, terminal multi-session + LAN server opt-in 0.0.0.0 two URLs QR ZXing open in browser keep-alive, outputs temporary RunArtifacts + RepoHygiene ~60 patterns incl .codec/ user's .gitignore wins, GitHub truth GitReadiness + PushOutcome + Publish POST /user/repos private ls-remote, feedback hardcoded DeveloperContact +91 62967 46606 / email + FeedbackScreen + exit survey + crash-log header-first + OpenInBrowser, share-readiness backup include-list-only FullBackupContent law crash-loop guard safe mode 3rd launch export-all over both roots 11 privacy rows RELEASE_NOTES template {{SHA256_LINES}}, file-system strength safe walk Throwable-safe TreeWalkPolicy ProjectLink persisted SAF grant noexec mirror, BETA B-1…B-8, TCC null on armeabi-v7a/x86.

---

## 1. Design

Before the course publishes a single chapter, the repo is re-read and every
claim the course may make is written down with a source. This gate exists
because the course (unlike the product pages) teaches *steps*, and a step
that doesn't exist on a fresh install breaks the whole promise of
self-sufficiency.

### What gets verified (minimum rows, v2.2 expanded to 15)

1. **Install paths v2.2** — release channel app-v* tags only, universal APK `CodeC-IDE-<version>-universal.apk` ONLY 6.6 MB signed non-debuggable -74% via R8+shrinkResources, per-ABI splits measured <2% and reverted because assets/tcc not filtered, SHA256 lines in release notes + versionCode + date + changelog template RELEASE_NOTES.md asserted by check_release_notes.sh, debug artifacts CodeC-IDE-debug/release for branch builds, in-app updater Settings → About → Check for updates looks only at app-v* releases compares numerically verifies sha256 refuses downgrade opens Releases page when no checksum, debug→release fresh install export first (BETA.md).
2. **Package list** — re-extract from `codec-packages/` build config (sha recorded); diff against W3.2's table; **note which tools exist for chapters 09, 10, 12, 15** (bash, python/python3, any networking tools, make/clang/ripgrep/tmux/nano, plus nodejs/npm etc if shipped).
3. **Engines & coverage v2.2** — Auto only (picker deleted Phase 21 D22, Termux card deleted Phase 38.2 D17 but mechanism stays TermuxCompiler fallback + RUN_COMMAND + <queries>), TCC coverage ANSI C + most C99, arm64-only Clang module, TCC null on armeabi-v7a/x86 by design, four setup steps appear in Output Panel exactly when build fails with Permission denied signature via CompilerRemediation → finishFailedBuild SYSTEM remedy line wording source TROUBLESHOOTING.md §27, CHECK BRIDGE no longer Settings button.
4. **Terminal facts** — `$PREFIX` path, `cc` = TCC, one-command-per-line, `./` rule, app-private project storage path + externalFilesDir candidate, input-programs-in-Term rule, multi-session TerminalSessionManager 8-cap, session switcher, progressive apt streaming, background survival.
5. **CodeCApi facts v2.2** — eight scripts: battery.status / sensor.read / tts.speak / camera.capture / intent.send + clipboard / notify / toast / share / open-url / vibrate (Phase 5.3, 4.7, 4.8, 18), output paths $PREFIX/tmp/codec-api/camera/ etc, markers NEED_PERMISSION: CAPTURING:, TTS cap 32 KiB, camera output-name rule [A-Za-z0-9._-] .jpg/.jpeg/.png.
6. **Editor facts v2.2** — autosave ~2s, extra-keys row ESC TAB CTRL ALT arrows + custom macros, Format clang-format bridge + C-indenter fallback, squiggle quick fix missing-; Ln/Col status bar, official file icons Seti MIT + Compose PathParser extension→icon map, typing feel keyboard stays open toggle + no gesture dismisses CodeC Keys while editing + smooth typing p95 + solid non-blinky caret while typing + no cursor on open until tap, ghost text GhostCompletion FULL/WORD/LINE TAB ▸ / →▸ word / caret-row pill / tap-ghost / HW Tab & Ctrl+→, suggestion strip StripContext Hidden|Run|Keys|Suggestions Run always wins ≥2 candidates ƒ/λ/≠ chips pinned ⌨ and ⌄ more caps tap-accept long-press tooltip swipe-down dismiss-per-identifier CodeCCompletionComponent gates sora panel to ⌄ more browse only, CompletionPolicy Enter sacred TAB tells truth, TextMate language-textmate MIT grammars 15 langs VS Code Dark+ dark_plus token colors lazy per language off UI thread, snippets 29 friendly-snippets MIT packs ~54KB deflated 84C/76Python/126HTML/156CSS/367JS/140TS/62MD/16shell + Emmet clean-room rank 0 markup ! > + ^ *n () .class #id [attr] {text} $ + CSS 82 abbrevs MAX_ITEMS 8→50 snippets≤40 identifiers≤6 keywords≤6 MAX_CHIPS 8 + ⌄ more replaceLength/caretOffset, LSP hand-rolled stdio JSON-RPC sora editor-lsp AAR gated minSdk26 vs 24, phone canvas NavBarPolicy auto-hides 5-tab bar while Keys/IME up thin handle, RUN chooser ProjectRunTarget.chooserDefault "Run default / Run open" EditorViewModel.runFile, space-in-path fix, missing main hint, CEntryWrapper wrapper.
7. **Web preview facts v2.2** — loopback server over project folder relative CSS/JS fetch data.json ES modules work live reload on save console output shows under page, plus LAN server opt-in 0.0.0.0 bind pool 8100–8199 never <1024 flask/fastapi/C templates honour CODEC_SERVER_HOST LanAddress pure + LanAddressProvider Android ServerEndpoints.of single place two URLs refuses unreachable LAN URL panel states why ServerSharePanel both URLs copy + ZXing core 3.5.4 QR Apache-2.0 About line + open in browser via OpenInBrowser/ShareActions copy fallback ServerRegistry one owner port truth + ServerHost one owner socket/process leaving editor keeps LAN serving while loopback dies with screen same RunForegroundService Serving <project> on <ip>:<port> + Stop ticker suppressed.
8. **Git UI facts v2.2** — Settings → GitHub Account, Clone from GitHub, Source Control pane per-file stage toggle stageFile/unstageFile, COMMIT & PUSH, unpushed badge amber ↑N when commits never reached remote, conflict grouping purple Mark Resolved block commit, branch with no upstream published on first push --set-upstream failed push never looks successful "Committed locally ✓ — NOT pushed: …" PUSH retry, Switch Branch branch list local+remote + New branch… stash/auto-restore, plus GitReadiness pre-flight is git installed · token stored · is repo · has remote · branch has upstream · anything to push pure data rendered before tap Install Git routed to Modules installer, clone error shows inside dialog not behind, PushOutcome/PushParser result card what pushed to which remote/branch Everything up-to-date * [new branch] or still local because X, Publish to GitHub via POST /user/repos private by default ls-remote-verified adopt-existing X-Accepted-GitHub-Permissions.
9. **Icon facts** — original >_ mark docs/icon/codec-mark.svg 108 viewport inside 66dp safe box + 33-unit safe circle farthest ~26.9 centered 54,54 flat #3DDC84 glyph + flat #101418 background, real monochrome layer white same two paths, adaptive XMLs rewired, ic_stat_codec bolder 24-grid silhouette, app_mark rounded tile + glyph About header, 10 PNG rasters 48/72/96/144/192 square/round + codec-512.png generated by scripts/render_icon.mjs node+sharp 0.35.4 BSD-3 pinned scripts/package.json committed outputs re-run twice byte-identical md5 guarded by check_icon_assets.sh CI step + IconAssetSetTest template webps deleted, RunForegroundService + TerminalForegroundService + CodecApiBridge switched to ic_stat_codec.
10. **Settings trim facts** — 13→11 sections Compiler Settings + Built-in Compiler + Termux Engine merged into one Compiler three real settings Engine prose one-sentence terminal-app promise TCC status line, whole Termux card deleted status row OPEN TERMUX CHECK BRIDGE How to enable together with TermuxUiState/loadTermuxState/buildTermuxStatusText/formatProbe + TermuxCompiler import, mechanism untouched, four steps moved to CompilerRemediation pure, audit SETTINGS_AUDIT.md 43 control rows deleted duplicate Appearance Terminal Theme dropdown duplicate Licenses item two store keys recent_files_csv write-only and smart_typing_delete_word never read deleted with four call sites SettingsKeysHaveReadersTest walks real chain 4 hops stores enumerated SettingsManager+ThemeManager+GitCredentialsStore SettingsAuditTest pins audit table.
11. **Outputs temporary facts** — RunArtifacts filesDir/CodeC/temp/runs/<stamp>/ + TempGc age/capacity/newest-N prunes on start and after Stop, RepoHygiene ~60 patterns incl .codec/ derived from CC0 github/gitignore templates plus CodeC own .codec/ .codec.json applied at single choke point stageAll git rm --cached already tracked what will be committed list before commit user's .gitignore always wins.
12. **Feedback facts** — FeedbackDraft → https://wa.me/<digits>?text= hardcoded DeveloperContact WHATSAPP_E164 916296746606 display +91 62967 46606 EMAIL chakraborttypabi2772006@gmail.com single source every channel reads reply-to fields + FeedbackStore + FeedbackContacts deleted contact DataStore keys gone exit-prompt switch moved to SettingsManager feedback_exit_prompt_enabled only feedback key FeedbackScreen own screen Settings one OPEN row audit control 46 exit survey back-at-root Enjoying CodeC? 💚 star row / SHARE EXPERIENCE / GIVE A REVIEW / NOT NOW / EXIT tap back again to exit outside taps disabled rating rides report info line · Rating: 4/5 NOTHING uploaded by itself GIVE A REVIEW opens public repo whole prompt off-able default ON, CrashLog.newestRecord one read filesDir/crash-log.txt header-first frame-capped COPY ALL yields complete record dialog title exception line versionName carries CI run number, OpenInBrowser.openOrCopy shared policy Phase 37 share row routes through it launch that no browser can serve copies link, no telemetry three honest disclosure lines above checkboxes ephemeral checkboxes privacy law, COPY REPORT / EMAIL / GITHUB ISSUE fallbacks repo public 2026-09-10 so issue link unconditional.
13. **Share-readiness facts** — universal APK 6.6 MB -74% 25 553 564 → 6 630 554 R8 release-only + shrinkResources + crunch proguard-rules.pro law file every keep needs named proven failure first jdt.annotation dontwarn run 34577896124 okhttp pair removed per-ABI splits built measured and reverted owner 0.94–1.77% vs 15% law assets/tcc/<abi> rides every split x86 kept measure-only release lane throwaway key uploads nothing, release channel app-v* tags only version guard notes template SHA256 lines idempotent attach UpdatePolicy name-grammar law first publish app-v1.3.17 run 34597577303 GREEN, backup XMLs include-list-only after FullBackupContent lint law exclude-under-include hard error runs 34561139415/34567771307 tuition crash-loop guard safe mode on 3rd failed launch export+report hand-off exportAllZip over both project roots 11 privacy rows audit-pinned telemetry scan check-run annotations as failure surface, RELEASE_NOTES template {{VERSION}} {{VERSION_CODE}} {{DATE}} {{SHA256_LINES}} {{CHANGELOG}} asserted by check_release_notes.sh, BETA.md B-1…B-8 known issues.
14. **File-system strength facts** — safe folder walk bounded Throwable-safe folder walk crash is StackOverflowError escaping unbounded copyDocumentChildren caught only as Exception then open-folder-as-project via ProjectLink + persisted SAF grant, TreeWalkPolicy budgets progress+cancel per-provider failure as message Throwable at boundary grant persisted takePersistableUriPermission never called today, ZIP path guarded MAX_ZIP_ENTRIES 10k MAX_ZIP_ENTRY_BYTES 128 MB total-bytes cap path-escape check, ProjectLink projectName treeUri + ProjectLinkPolicy.decide take/refuse/re-pick when grant gone noexec physics emulated storage mounted noexec so linked project runs from internal mirror sync in on open/save push-set on save-back excludes CodeC own outputs 39.1 must land first, all-files-access open in place variant considered and rejected as default.
15. **BETA facts** — B-1 huge folder stalls single-threaded storage document APIs slow per file open subfolder, B-2 32-bit ARM older tablets universal APK includes native libs B-3 long-running builds die screen off keep notification visible, B-4 git push asks key again after update credentials store wiped backup restore partial reconnect, B-5 Parse error package appears invalid download interrupted updater verifies SHA-256 before install since 42.1, B-6 cursor/highlight wrong very long lines >10kB single line editor line-length guard 39.x readable substitute split line file on disk untouched display throttle never edit, B-7 crash-loop after update app closes splash twice persisted settings mismatched build 3rd launch Try starting without my settings, B-8 32-bit ARM built-in C compiler not available bundled offline TCC toolchain ships only arm64-v8a/x86_64 Phase 33.3 typed null EmbeddedCompiler.tccBinary() returns null.

### Gate outcomes

- **Green** → chapter set locked at the 17 in plan §3.2; O6 closed;
  W4.1–W4.8 proceed.
- **Amber** (a chapter's premise is thin — e.g. no networking tools for
  ch-12) → the affected part doc already contains the fallback outline
  (see `PART_5_6_CH12.md` variant B); chapter count stays 17 unless the
  owner re-scopes.
- **Red** (a chapter's premise is false) → chapter cut/re-scoped, `DECISIONS.md`
  entry, plan §3.2 updated, O6 closed with the new set.

## 2. Implementation steps

1. Read `README.md` fully; re-extract the package config list.
2. Write the Verified Facts Table (fact · value · source file+section ·
   sha/date) into `web_docs/chat-web4/VERIFIED_FACTS.md`.
3. Diff against W3.2's package table; resolve any drift (drift = repo
   changed since W3 — the repo wins; update W3.2's page in the same commit
   if needed — same workstream, allowed).
4. Record the locked chapter set; close O6 in `DECISIONS.md`; update
   `NEXT_STEPS.md` head state.

## 3. Exit condition

```text
1. VERIFIED_FACTS.md committed with all 8 row-groups complete; every row
   has a source.
2. Package list diff against W3.2 recorded (clean or drift handled).
3. Locked chapter set (expected 17) recorded; O6 closed in DECISIONS.md.
4. Any plan §3.2 change made in the same commit.
```
