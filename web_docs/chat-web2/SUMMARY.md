# chat-web2 — W0.3 sync session (2026-09-12)

## What the owner asked

> "Check the new updates in the project from the docs folder then update the web-docs and web-prompmt"

Branch: `arena/01a09389-codec` (session branch, clean at start, tip `f3a6e32` Merge PR #72 arena/01a08e79-codec → main).

## Research done (verify-first)

1. **App head:** `git log --oneline -20` → main at `f3a6e32` (PR #72 merged, Phase 42). `docs/NEXT_STEPS.md` head line: Phase 41 COMPLETE & MERGED via PR #70, next Start Phase 42, queue 42→43. `docs/JOURNEY.md` tail §58: Phase 42 COMPLETE (release channel app-v1.3.17 published run 34597577303, weight 25.5 MB → 6.6 MB -74% R8+shrink, per-ABI splits measured 0.94–1.77% and reverted because assets/tcc not filtered, okhttp removed, backup include-list-only FullBackupContent law, crash-loop guard safe mode 3rd launch, export-all ZIP, 11 privacy rows, RELEASE_NOTES template {{SHA256_LINES}}, BETA.md) merged via PR #71 merge commit 3c983c6 main CI 34602562874. Phase 43 PLANNED (safe folder walk Throwable-safe TreeWalkPolicy + ProjectLink persisted SAF grant noexec mirror).
2. **Phases 34–43 docs:** `docs/PHASE34_37_ROADMAP.md`, `docs/PHASE38_43_ROADMAP.md`, `docs/PHASE38_43_OSS_RESEARCH.md`, `chat-phase34/` … `chat-phase43/` READMEs + PART docs. Extracted:
   - 34 official file icons Seti MIT + Compose PathParser
   - 35 editor typing feel (keyboard stay open toggle, smooth typing p95, non-blinky caret while typing, no cursor on open until tap)
   - 36 terminal speed & feel (TerminalSessionManager N concurrent PTY 8-cap anyAlive wake lock, session switcher dropdown + rename/close, progressive apt streaming, background survival, session-switch redraws)
   - 37 device as server LAN (opt-in LanSharePolicy OFF default not persisted, WebPreviewServer binds 0.0.0.0 pool 8100–8199 never <1024, flask/fastapi/C templates honour CODEC_SERVER_HOST, LanAddress pure + LanAddressProvider Android, ServerEndpoints.of(port,bind,lanAddress,lanShared) single place two URLs refuses unreachable LAN URL panel states why, ServerSharePanel both URLs copy + ZXing core 3.5.4 QR Apache-2.0 + open in browser via OpenInBrowser/ShareActions copy fallback, ServerRegistry one owner port truth + ServerHost one owner socket/process leaving editor keeps LAN serving while loopback dies with screen same RunForegroundService Serving <project> on <ip>:<port> + Stop ticker suppressed, 68 host cases)
   - 38 identity >_ mark docs/icon/codec-mark.svg 108 viewport safe-zone-verified farthest ~26.9 centered 54,54 flat #3DDC84 glyph + flat #101418 background, real monochrome layer white same two paths, adaptive XMLs rewired, ic_stat_codec bolder 24-grid silhouette, app_mark rounded tile + glyph About header, 10 PNG rasters 48/72/96/144/192 square/round + codec-512.png generated by scripts/render_icon.mjs node+sharp 0.35.4 deterministic md5 guarded by check_icon_assets.sh CI step + IconAssetSetTest, template webps deleted, RunForegroundService + TerminalForegroundService + CodecApiBridge switched to ic_stat_codec, About header shows mark+name+tagline, Settings 13→11 Compiler Settings + Built-in Compiler + Termux Engine merged into one Compiler, whole Termux card deleted TermuxUiState/loadTermuxState/buildTermuxStatusText/formatProbe + TermuxCompiler import removed, mechanism untouched TermuxCompiler fallback + RUN_COMMAND + <queries> stay, four steps moved to CompilerRemediation pure failed build exec Permission denied signature gets SYSTEM remedy line Output Panel EditorViewModel.finishFailedBuild wording source TROUBLESHOOTING.md §27 CompilerRemediationTest pins both directions, audit SETTINGS_AUDIT.md 43 control rows deleted duplicate Appearance Terminal Theme dropdown duplicate Licenses item two store keys recent_files_csv write-only and smart_typing_delete_word never read deleted, SettingsKeysHaveReadersTest walks real chain 4 hops, SettingsAuditTest pins audit table, 45 new host cases pre-validated 45/45, CI green Build APK 34442522565 tip ce1a38c, device round PASSED owner All device test pass, merged PR #64 main dcd65b4
   - 39 outputs temporary RunArtifacts filesDir/CodeC/temp/runs/<stamp>/ + TempGc age/capacity/newest-N prunes on start and after Stop, RepoHygiene ~60 patterns incl .codec/ derived CC0 github/gitignore templates plus CodeC own .codec/ .codec.json applied at single choke point stageAll git rm --cached already tracked what will be committed list before commit user's .gitignore always wins
   - 40 GitHub truth GitReadiness/GitBlocker pre-flight is git installed · token stored · is repo · has remote · branch has upstream · anything to push pure data rendered before tap Install Git routed to Modules installer, clone error shows inside dialog not behind (FileManagerScreen closes showCloneDialog only in success callback), PushOutcome/PushParser result card what pushed to which remote/branch Everything up-to-date * [new branch] or still local because X, Publish to GitHub via POST /user/repos private by default ls-remote-verified adopt-existing X-Accepted-GitHub-Permissions
   - 41 feedback FeedbackDraft → https://wa.me/<digits>?text= hardcoded DeveloperContact WHATSAPP_E164 916296746606 display +91 62967 46606 EMAIL chakraborttypabi2772006@gmail.com single source every channel reads reply-to fields + FeedbackStore + FeedbackContacts deleted, FeedbackScreen own screen Settings one OPEN row audit control 46 exit survey back-at-root Enjoying CodeC? 💚 star row / SHARE EXPERIENCE / GIVE A REVIEW / NOT NOW / EXIT tap back again to exit outside taps disabled rating rides info line · Rating: 4/5 NOTHING uploaded by itself GIVE A REVIEW opens public repo whole prompt off-able feedback_exit_prompt_enabled default ON, CrashLog.newestRecord one read filesDir/crash-log.txt header-first frame-capped COPY ALL yields complete record dialog title exception line versionName carries CI run number, OpenInBrowser.openOrCopy shared policy Phase 37 share row routes through it, no telemetry three honest disclosure lines ephemeral checkboxes, COPY REPORT / EMAIL / GITHUB ISSUE fallbacks repo public 2026-09-10 so issue link unconditional, merged PR #70
   - 42 share-readiness 42.1 release channel publish lane app-v* tags only version guard notes template SHA256 lines idempotent attach UpdatePolicy name-grammar law first publish app-v1.3.17 run 34597577303 GREEN shipped CodeC-IDE-1.3.17-universal.apk three attempts recorded attempt1 signed-assemble failed opaquely error-surfacer itself crashed → lane now annotates filtered tail surfacer crash-proof attempts2-3 real error Failed to read key upload null mobile-clipboard whitespace inside secret → owner re-entered published, 42.2 weight R8 release-only + shrinkResources + crunch proguard-rules.pro law file every keep needs named proven failure first jdt.annotation dontwarn run 34577896124 okhttp pair removed per-ABI splits built measured and reverted owner 0.94–1.77% vs 15% law assets/tcc/<abi> rides every split x86 kept measure-only release lane throwaway key uploads nothing, final record 25 553 564 → 6 630 554 B -74% non-debuggable every run mapping.txt 54.9 MB CI-only, 42.3 launch safety backup XMLs include-list-only after FullBackupContent lint law exclude-under-include hard error runs 34561139415/34567771307 tuition crash-loop guard safe mode on 3rd failed launch export+report hand-off exportAllZip over both project roots 11 privacy rows audit-pinned telemetry scan check-run annotations as failure surface, device round release 4/4 pass fresh install editor across language matrix About fingerprint offline C compile updater up to date API 24/26 install + 42.3 runbook carried to BETA.md, merged PR #71 merge commit 3c983c6 main CI 34602562874
   - 43 planned safe folder walk bounded Throwable-safe TreeWalkPolicy budgets progress+cancel per-provider failure as message Throwable at boundary grant persisted takePersistableUriPermission never called today ZIP caps 10k/128 MB, ProjectLink projectName treeUri + ProjectLinkPolicy.decide take/refuse/re-pick when grant gone noexec mirror sync in on open/save
3. **README.md** new install channel universal APK only signed non-debuggable SHA256 lines debug artifacts CodeC-IDE-debug/release for branch builds in-app updater Settings → About → Check for updates looks only at app-v* releases compares numerically verifies sha256 refuses downgrade opens Releases page when no checksum never installs userland-* bootstrap as app never phones home, debug→release fresh install export first, device support arm64 best x86_64 emulator via built-in TCC 32-bit built-in TCC null → Clang module or Termux fallback, universal APK only per-ABI splits reverted assets/tcc not filtered, Auto engine only picker deleted Phase 21 Termux card deleted Phase 38.2 but mechanism stays four steps appear in Output Panel exactly when needed TROUBLESHOOTING.md §27, original >_ mark docs/icon/codec-512.png, Settings trim 13→11, official file icons, typing feel, LAN server, outputs temporary, GitHub truth, feedback hardcoded, export-all, backup include-list, crash-loop guard, safe walk planned, targetSdk 28 deliberate, R8 weight, RELEASE_NOTES template, BETA.md B-1…B-8.
4. **Website current:** `web_docs/` at v2.1 (2026-09-02) — 7 product pages + 17 chapters spec'd, 35 docs, no code, PR #41 history. `web_prompt.md` v2.1 facts outdated (Actions artifact CodeC-IDE, 4 engines selectable, no icon, no file icons, no LAN, no outputs temporary, no GitHub truth, no feedback, no backup/crash-loop/export-all, no safe walk, no universal APK).

## What was changed (v2.2 sync)

| File | Change |
|---|---|
| `web_docs/WEBSITE_PLAN.md` | v2.1 → v2.2: status 2026-09-12, §1 What we are building updated install channel universal APK 6.6 MB signed SHA256 + updater version guard + debug vs release + per-ABI reverted reason, §3.0 shared chrome >_ mark icon, §3.1 Home 6 cards enriched file icons LAN safe features + footnote 6.6 MB universal >_ mark, /install rewritten release-first universal APK only + debug artifacts + in-app updater SHA256 version guard refusal debug→release fresh install export-all + device table arm64 best x86_64 good 32-bit TCC null + Termux fallback automatic four steps appear in Output Panel not Settings card, /start first-hour tiles + open into last file + autosave + multi-session + LAN share + export-all + backup + crash-loop + feedback + inline PTY input + run keys, /engines Auto only no picker no Termux card fallback automatic CHECK BRIDGE removed error-path, /packages same + config sha, /faq expanded 9→15 blocks BETA B-1…B-8 + debug/release + signature change + backup include-list + crash-loop safe mode + export-all + huge folder + 32-bit null + feedback hardcoded + outputs temporary, /about >_ mark + Settings trim + feedback hardcoded + LAN + file icons + outputs temporary + GitHub truth + backup + crash-loop + export-all + story phases 0–43 + engineering facts icon + TCC + signed repo + CI + targetSdk 28 + R8 + backup + crash-log + feedback + no telemetry, §3.2 chapter table enriched with Phases 34–43 notes (01 release channel export-all backup, 02 Auto, 03 multi-session + terminal speed, 04 Auto only no picker Termux card deleted CHECK BRIDGE error-path, 05 quick actions, 06 safe walk + ProjectLink + export-all + two roots + ZIP caps + outputs temporary, 07 editor Seti icons + typing feel + ghost + strip + snippets 29 packs + Emmet + TextMate + 50 items + CodeC Keys, 08 TCC-safe law, 09–10 same, 11 GitReadiness + push truth + publish, 12 LAN server QR open-in-browser, 13 8 scripts, 14 LAN share, 15 export-all + backup + crash-loop + feedback + fingerprint, 16 LAN share, 17 BETA + crash-log + feedback), §4 content rules source order README → TROUBLESHOOTING → BETA → RELEASE_NOTES → JOURNEY, §5 self-dependent unchanged, §6 design >_ mark favicon, §7 repo layout icon assets, §8 deployment unchanged, §9 phases queue notes v2.2 updates W2.1 install + W3.1 engines + W3.3 FAQ + W3.4 About + W4.6 ch-04 + W4.8 ch-06 + W5.6 ch-12 + W6.1 ch-13 + W6.3 ch-15 + W6.5 ch-17 updated, W4.2 verification gate must re-verify new facts, §10 acceptance criteria +9 new facts, §11 open questions O8 added feedback contact display on website? |
| `web_docs/DECISIONS.md` | D14–D22 added: D14 Phase 21 Auto only, D15 Phases 34–37 UX/UI file icons + typing feel + terminal speed + LAN server, D16 Phase 38 identity >_ mark + Settings trim, D17 Phase 39 outputs temporary, D18 Phase 40 GitHub truth, D19 Phase 41 feedback hardcoded, D20 Phase 42 share-readiness universal APK 6.6 MB -74% + backup include-list + crash-loop + export-all + RELEASE_NOTES template, D21 Phase 43 planned safe walk + ProjectLink, D22 website spec v2.2 sync. O6 updated (enriched but count stays 17), O8 added. |
| `web_docs/NEXT_STEPS.md` | Head state v2.2 2026-09-12: W0 complete zero code all phases spec'd and synced with app Phases 21–43 (list 34–43 facts + 21–33), install facts universal APK 6.6 MB signed SHA256 + updater version guard + debug vs release + export-all + backup + crash-loop, engines Auto only no picker Termux card deleted fallback automatic four steps appear in Output Panel, repo facts >_ mark + Settings trim + file icons + LAN + outputs temporary + GitHub truth + feedback hardcoded + export-all + backup + crash-loop + safe walk planned, nothing built no website folder, app head Phase 42 merged PR #71 app-v1.3.17 published Phase 43 planned. Owner command table updated to v2.2, phase queue updated with v2.2 notes, verification before session updated to re-read README + BETA + RELEASE_NOTES + TROUBLESHOOTING, history pointer W0.3. |
| `web_docs/WEB_JOURNEY.md` | Added W0.3 entry 16–18: owner asks sync, app delta researched (Phases 21–43 facts extracted with evidence), website specs updated to v2.2 (list of files changed + new facts). |
| `web_prompt.md` | v2.1 → v2.2: WHERE THE WEBSITE STANDS updated to W0+W0.1+W0.2+W0.3 complete, all phases spec'd and synced with Phases 21–43, app head Phase 42 merged PR #71 app-v1.3.17 universal 6.6 MB Phase 43 planned, phase specs note W4.2 must re-verify v2.2 facts, WHAT THE OWNER MUST SAY TO PROCEED updated with v2.2 chapter enrichments, FACTS THAT MUST NOT REGRESS expanded: stack locked, self-dependent law, learning wing updated with Auto only + file icons + typing feel + ghost + strip + snippets 29 packs + Emmet + TextMate + 50 items + CodeC Keys + GitReadiness + push truth + publish + LAN server + QR + open-in-browser + keep-alive + 8 scripts + export-all + backup + crash-loop + feedback + BETA, content source truth order README → TROUBLESHOOTING → BETA → RELEASE_NOTES → JOURNEY, install facts v2.2 universal APK ONLY 6.6 MB signed non-debuggable SHA256 lines debug artifacts updater version guard + checksum + refusal debug→release fresh install, repo facts v2.2 >_ mark + Auto only + TCC null + LAN + signed packages + Spck editor + Projects hub + honest git + outputs temporary + safe walk + ProjectLink + export-all + backup + crash-loop + feedback hardcoded + targetSdk 28 + R8 + RELEASE_NOTES template + BETA B-1…B-8. |
| `web_docs/README.md` | WEBSITE_PLAN.md entry v2 → v2.2 with sync note, clean-room source list expanded README + TROUBLESHOOTING + BETA + RELEASE_NOTES + JOURNEY, ground rule 6 added v2.2 sync note. |
| `web_docs/web-phase2/PART_2_1_INSTALL.md` | Rewritten v2.2: release-first universal APK ONLY 6.6 MB signed SHA256 versionCode date changelog template asserted by check_release_notes.sh, per-ABI splits measured <2% and reverted assets/tcc not filtered, debug artifacts, in-app updater Settings → About → Check for updates looks only at app-v* releases compares numerically verifies sha256 refuses downgrade opens Releases page when no checksum, debug→release fresh install export-all, device table arm64 best x86_64 good 32-bit TCC null, Termux fallback automatic four steps appear in Output Panel not Settings card (Phase 38.2 deleted), external links only two Termux links allowed §5.2. |
| `web_docs/web-phase3/PART_3_1_ENGINES.md` | Rewritten v2.2: Auto only only mode, picker deleted Phase 21, Termux card deleted Phase 38.2 but mechanism stays, table conceptual not selectable, TCC null on armeabi-v7a/x86, universal APK 6.6 MB per-ABI reverted, four setup steps appear in Output Panel via CompilerRemediation → finishFailedBuild SYSTEM remedy line wording source TROUBLESHOOTING §27, CHECK BRIDGE no longer Settings button. |
| `web_docs/web-phase3/PART_3_3_FAQ.md` | Expanded 9→15 blocks v2.2: Permission denied W^X targetSdk 28 + noexec + update + uninstall+reinstall once + Termux fallback automatic Output Panel, Exec format error ARM64 + x86_64 TCC assets/tcc 3.7M+3.6M + jniLibs 581600+405352 B Clang module arm64-only per-ABI reverted, Runtime libraries missing, hangs 30s/10s caps scanf in Term, Do I need Termux No + no Settings card, Keyboard input extra-keys + CodeC Keys auto-close hold-repeat swipe popups + typing feel Phase 35, Where do my projects live app-private + externalFilesDir candidate + export-all ZIP over both roots byte-identical + safe walk + ProjectLink + outputs temporary RunArtifacts + RepoHygiene ~60 patterns .codec/ user's .gitignore wins, Backup & restore include-list-only FullBackupContent law projects only token/userland not backed up, Crash-loop safe mode 3rd launch Try starting without my settings, Debug vs release debuggable flag run-as risk signature change fresh install R8 25.5 MB → 6.6 MB -74% mapping.txt CI-only non-debuggable every run aapt, Parse error SHA256 verification since 42.1, Huge folder + cursor wrong + 32-bit null B-1…B-8, Where report bug feedback hardcoded +91 62967 46606 / email + FeedbackScreen + exit survey + crash-log header-first COPY ALL + OpenInBrowser + no telemetry + GitHub Issues, Outputs in my repo .codec folder RunArtifacts + RepoHygiene. |
| `web_docs/web-phase3/PART_3_4_ABOUT.md` | Rewritten v2.2: H1 6.6 MB universal offline C no setup + original >_ mark docs/icon/codec-mark.svg 108 viewport safe-zone farthest ~26.9 flat #3DDC84 + #101418 background real monochrome white same two paths adaptive XMLs rewired ic_stat_codec bolder 24-grid silhouette app_mark rounded tile + glyph About header 10 PNG rasters + codec-512.png sharp 0.35.4 deterministic md5 template webps deleted, feature tour enriched file icons Seti + typing feel + ghost + strip + CompletionPolicy + TextMate + snippets 29 packs + Emmet + MAX_ITEMS 50 + LSP + phone canvas NavBarPolicy + RUN chooser ProjectRunTarget + first-hour tiles WelcomeStarters + Projects hub safe walk TreeWalkPolicy + ProjectLink persisted SAF grant noexec mirror + export-all + ZIP caps + honest git + GitReadiness + PushOutcome + Publish + LAN server opt-in 0.0.0.0 + QR ZXing + ServerRegistry/ServerHost + open in browser + CodeCApi 8 scripts + outputs temporary + feedback hardcoded + safe & shareable universal APK + backup + crash-loop + export-all + privacy rows + RELEASE_NOTES template + BETA + DATA_AND_PRIVACY, story phases 0–43 PR #71 merge commit 3c983c6, engineering facts icon + TCC + signed repo + CI + targetSdk 28 deliberate + R8 law file + backup XMLs include-list-only + crash-log header-first + FeedbackDraft wa.me hardcoded + no telemetry. |
| `web_docs/web-phase4/PART_4_2_VERIFY_GATE.md` | Expanded 8→15 rows v2.2: install paths universal APK 6.6 MB per-ABI reverted SHA256 template debug artifacts updater version guard debug→release fresh install, package list, engines Auto only picker deleted Termux card deleted fallback automatic four steps appear in Output Panel TCC null, terminal multi-session, CodeCApi 8 scripts, editor official file icons + typing feel + ghost + strip + TextMate + snippets 29 packs + Emmet + MAX_ITEMS 50 + LSP + phone canvas + RUN chooser, web preview loopback + LAN server opt-in 0.0.0.0 pool 8100–8199 ServerEndpoints two URLs QR ZXing open in browser ServerRegistry/Host keep-alive, git GitReadiness + PushOutcome + Publish, icon >_ mark + ic_stat_codec, Settings trim 13→11 + SETTINGS_AUDIT 43 rows + SettingsKeysHaveReadersTest, outputs temporary RunArtifacts + RepoHygiene, feedback hardcoded + FeedbackScreen + exit survey + CrashLog + OpenInBrowser no telemetry, share-readiness universal APK -74% R8 backup include-list crash-loop export-all privacy rows RELEASE_NOTES template, file-system strength safe walk TreeWalkPolicy ProjectLink persisted SAF grant noexec mirror, BETA B-1…B-8. |
| `web_docs/web-phase1/PART_1_2_HOME.md` | Cards updated v2.2: built-in compiler universal APK 6.6 MB -74% R8, real terminal + LAN server opt-in 0.0.0.0 two URLs QR ZXing open in browser keep-alive, package hub quick actions, Spck editor file icons Seti + ghost TAB + strip ƒ/λ/≠ + ⌄ more + 29 snippet packs + Emmet + TextMate Dark+ + typing feel + honest git + autosave + safe walk + ProjectLink + export-all, web preview loopback+LAN, always updatable & safe updater SHA256 version guard refusal debug→release + export-all + backup include-list + crash-loop guard safe mode + feedback hardcoded +91 62967 46606, learning banner 6.6 MB universal offline C feedback reaches developer, footnote >_ mark icon source. |
| `web_docs/web-phase2/PART_2_2_START.md` | Steps updated v2.2: H1 6.6 MB universal offline C no setup, first compile Auto only no picker, terminal loop projects two roots noexec mirror multi-session, input programs inline PTY input + run keys, package 1-tap INSTALL/RUN live badges quick actions extra-keys macros, web preview + LAN share opt-in 0.0.0.0 pool 8100–8199 two URLs QR open in browser keep-alive, where things live first-hour tiles + open into last file + autosave + export-all + backup + crash-loop + feedback hardcoded, cross-links install universal + engines Auto only + FAQ BETA. |
| `web_docs/web-phase4/PART_4_6_CH04.md` | Rewritten v2.2: goal box Auto fallback chain + where Termux guidance appears now error path not Settings, steps Auto ONLY since Phase 21 picker deleted Termux card deleted Phase 38.2, table conceptual Auto/Built-in/Bundled/Termux TCC null Clang arm64-only per-ABI reverted, TCC covers ANSI C + most C99 static musl native lib dir targetSdk 28, where Termux guidance appears now not Settings but Output Panel CompilerRemediation failed build Permission denied signature SYSTEM remedy line EditorViewModel.finishFailedBuild wording source TROUBLESHOOTING §27, know your device v2.2 arm64 best x86_64 good 32-bit null, no switching on purpose Auto does it, try it no picker + struct+function + read TROUBLESHOOTING §27, mistakes Clang module x86 + Auto fallback success + CHECK BRIDGE deleted + picker deleted + built-in null on 32-bit. |
| `web_docs/web-phase4/PART_4_8_CH06.md` | Rewritten v2.2: goal box safe walk + ProjectLink + export-all + two roots + outputs temporary never in repo, steps two home bases single files vs projects filesDir + externalFilesDir candidate noexec mirror, Projects hub card list type mark official file icons Seti + first-hour tiles, create project, move file Save to project tabs re-key terminal follows open into last file autosave ~2s, import & export v2.2 Import ZIP caps 10k/128 MB total-bytes cap path-escape finally delete + Open Folder SAF ActivityResultContracts.OpenDocumentTree safe walk plain recursion no visited set no budget no cancellation opens every file stream serially catches only Exception StackOverflowError Error kills app replaced iterative TreeWalkPolicy budgets progress+cancel per-provider failure Throwable boundary grant persisted takePersistableUriPermission zero call sites Phase 43.1 + export-all over both roots byte-identical ProjectTransferExportAllTest + ZIP share FileProvider, Open folder as project ProjectLink + ProjectLinkPolicy persisted SAF grant mandatory reboot flags on intent not grant kill app reopen still works revoke permission says link gone offers re-pick must not crash, what git sees RunArtifacts temp/runs/<stamp>/ + TempGc + RepoHygiene ~60 patterns .codec/ .codec.json applied stageAll git rm --cached already tracked what will be committed list before commit user's .gitignore always wins. |
| `web_docs/web-phase4/*`, `web-phase5/*`, `web-phase6/*` (other PART docs) | Added v2.2 update note via batch sed: synced with Phases 21–43 universal APK 6.6 MB Auto only >_ mark file icons typing feel terminal multi-session + LAN server opt-in 0.0.0.0 two URLs QR ZXing open-in-browser keep-alive outputs temporary GitHub truth feedback hardcoded +91 62967 46606 / email + FeedbackScreen + exit survey + crash-log header-first + OpenInBrowser no telemetry backup include-list FullBackupContent crash-loop safe mode export-all RELEASE_NOTES template safe walk ProjectLink persisted SAF grant noexec mirror BETA B-1…B-8 TCC null. |

## Evidence

- `git status` shows modified: `web_docs/WEBSITE_PLAN.md`, `DECISIONS.md`, `NEXT_STEPS.md`, `WEB_JOURNEY.md`, `web_prompt.md`, `web_docs/README.md`, `web-phase2/PART_2_1_INSTALL.md`, `web-phase3/PART_3_1_ENGINES.md`, `PART_3_3_FAQ.md`, `PART_3_4_ABOUT.md`, `web-phase1/PART_1_2_HOME.md`, `web-phase2/PART_2_2_START.md`, `web-phase4/PART_4_2_VERIFY_GATE.md`, `PART_4_6_CH04.md`, `PART_4_8_CH06.md`, plus 18 PART docs with v2.2 note.
- No `website/` folder exists, no app code, `docs/` untouched, no workflow changes.
- Verified facts table will be committed in W4.2 (future), but v2.2 facts recorded here as interim source.
- `git diff --stat` shows only `web_docs/` + `web_prompt.md` changes — clean-room boundary honoured.

## Exit condition for this sync (W0.3)

```text
1. WEBSITE_PLAN.md v2.2 committed with Phases 21–43 facts: universal APK 6.6 MB signed SHA256, per-ABI reverted reason, Auto engine only picker deleted Termux card deleted fallback automatic four steps appear in Output Panel, >_ mark icon + ic_stat_codec, official file icons, typing feel, LAN server opt-in 0.0.0.0 two URLs QR ZXing open in browser keep-alive, outputs temporary RunArtifacts+RepoHygiene, GitHub truth GitReadiness+PushOutcome+Publish, feedback hardcoded +91 62967 46606 / email + FeedbackScreen + exit survey + crash-log header-first + OpenInBrowser no telemetry, backup include-list FullBackupContent crash-loop guard safe mode export-all RELEASE_NOTES template, safe walk TreeWalkPolicy ProjectLink persisted SAF grant noexec mirror, BETA B-1…B-8, TCC null.
2. DECISIONS.md D14–D22 + O8 added.
3. NEXT_STEPS.md head state v2.2 + phase queue updated.
4. WEB_JOURNEY.md W0.3 entry committed.
5. web_prompt.md v2.2 committed with updated install facts + repo facts + engine facts.
6. Critical product pages PART docs (install, engines, FAQ, about, home, start, ch-04, ch-06, verify gate) updated to v2.2.
7. Remaining chapter PART docs carry v2.2 note via batch.
8. No code written, strict rule held, no app code touched, no docs/ touched, no website/ folder.
→ W0.3 COMPLETE; report + stop at merge gate.
```
