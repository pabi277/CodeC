# CodeC Website Phase W3.4 — About CodeC (`/about`)

**Status:** 📋 **PLANNED** · **Cost:** `[static]` · **Effort:** M
· **Depends on:** W1
· **Target file:** `website/about.html`

> Source: `README.md` (whole), `docs/JOURNEY.md` (story phases 0–43), `docs/TERMINAL_PLAN.md` + `docs/chat-phase3/` (engineering facts) + `docs/chat-phase38/PART_38_1_APP_ICON.md` + `PART_38_2_SETTINGS_TRIM.md` (identity >_ mark + Settings trim) + `docs/chat-phase39/` + `chat-phase40/` + `chat-phase41/` + `chat-phase42/` + `chat-phase43/` (outputs temporary, GitHub truth, feedback, share-readiness, file-system strength) + `docs/BETA.md` + `docs/RELEASE_NOTES.md` + `docs/DATA_AND_PRIVACY.md` + `docs/PHASE34_37_ROADMAP.md` + `docs/PHASE38_43_ROADMAP.md`.
> **v2.2 update:** original >_ mark, Settings 13→11 Termux card deleted, official file icons, typing feel, LAN server, outputs temporary, GitHub truth, feedback hardcoded, export-all, backup include-list, crash-loop guard, universal APK 6.6 MB, targetSdk 28 deliberate, R8 weight, etc.

---

## 1. Design — page structure

1. **H1:** "CodeC is a C programming IDE for Android — 6.6 MB universal, offline C, no setup." One section: what it is (write C in projects or single files, tap RUN, or type `cc file.c` in in-app terminal, Python/JS/HTML via Packages tab, HTML preview served by loopback + LAN) and who it's for (learners, tinkerers, people who want terminal + compiler + packages on phone zero setup). Mention **original >_ mark** (Phase 38.1) — caret + underscore editor + terminal no robot no vendor glyph lives once in `docs/icon/codec-mark.svg` 108-viewport inside 66dp safe box + 33-unit safe circle farthest ~26.9 centered 54,54 flat #3DDC84 glyph + flat #101418 background, real monochrome layer, 10 PNG rasters 48/72/96/144/192 square/round + codec-512.png generated by scripts/render_icon.mjs node+sharp 0.35.4 BSD-3 pinned scripts/package.json committed outputs re-run twice byte-identical md5 guarded by check_icon_assets.sh CI step + IconAssetSetTest template webps deleted, ic_stat_codec notification silhouette replacing ic_launcher_foreground in both foreground services and system ic_dialog_info in CodecApiBridge, app_mark in About header, README logo.

2. **Feature tour** (short bullets, each linking relevant page, v2.2 enriched):
   - **Spck-grade editor:** file tree with git status letters + **official file icons** Seti MIT + Compose PathParser (Phase 34) extension→icon map LanguageRegistry types + common assets applied Files tab tree + editor drawer tree + editor tab bar + hub/drawer file glyph, tabs, extra-keys row + **CodeC Keys** auto-close + hold-repeat + swipe popups + JSON user sets, autosave ~2s, error squiggles tap-to-inspect + missing-; quick fix, **typing feel** Phase 35 (keyboard stays open Settings toggle + no gesture dismisses CodeC Keys while editing, smooth typing measure p95, solid non-blinky caret while typing kill re-animation VM→sora caret replay, no caret on open until tap), **ghost text** Phase 27 (GhostCompletion FULL/WORD/LINE accept + instant shrink rendered as point-anchored inlay hint via GhostHintRenderer comment@38% per G5 TAB ▸ cap / →▸ word cap / caret-row Tab pill / tap-ghost / HW Tab & Ctrl+→ scroll+composing clear it G4/G7), **suggestion strip** Phase 27 (StripContext Hidden|Run|Keys|Suggestions Run always wins 23.2 re-pinned ≥2 candidates → ƒ/λ/≠ chips with pinned ⌨ and ⌄ more caps tap-accept long-press tooltip swipe-down dismiss-per-identifier CodeCCompletionComponent gates sora native panel to ⌄ more browse sessions only), **CompletionPolicy** law (surfaceFor+decide Enter sacred TAB tells truth dismissal per-identifier master off zero chrome/computation) + Settings → Editor Settings rows, **TextMate** Phase 29 (sora language-textmate MIT grammars C/C++/Python/JS/TS/HTML/CSS/JSON/Shell/MD/Go/Rust/PHP/Ruby/Lua/XML/YAML default VS Code Dark+ real dark_plus token colors Monokai Dracula GitHub Dark remain Settings grammars load once per process lazily per language off UI thread), **Snippets, Emmet & suggestions** Phase 30 (29 friendly-snippets MIT packs bundled APK C/C++/Python/JS/TS/React/HTML/CSS/Go/Rust/PHP/Ruby/Lua/MD/Shell no download no server typing for #inc def main offers real snippet parks caret at first hole HTML CSS JSX understand Emmet clean-room built in ul>li*3 div.card>p{Hello} or ! fresh line expands full markup m10 d:f p10-20 expand CSS declarations up to 50 candidates per keystroke 8 chips above keyboard ⌄ more for rest plus inline ghost TAB ▸ accepts Enter always inserts newline never accepts accepting replaces exactly what you typed #in → #include <stdio.h> built-in CodeC Keys), **LSP as Packages** Phase 31 (hand-rolled LSP stdio JSON-RPC sora editor-lsp AAR gated minSdk26 vs 24 chip strip + ghost merge LSP on EditorViewModel.refreshCompletionItems), **Phone canvas** Phase 32 (NavBarPolicy auto-hides 5-tab bar in editor while Keys/IME up thin tap/swipe handle to reveal it pure EditorChromeState bridge, chips-as-row-0, OutputDiagnosticTarget diagnostic tap land in user's file), **RUN chooser** Phase 33 (default run file is USER choice make default run option as user task if user set any default file than it will open with option default or current file RUN runs open file by own type .c/.py/.js in web project compiles/runs in panel never previews index.html if user set launch default ⋮ → Set as launch default that differs from open file RUN asks Run default / Run open pure ProjectRunTarget.chooserDefault EditorViewModel.runFile(context,target) runs chosen file without switching tabs, space-in-path fix ccScript rebuilds args set -- "$@" "$arg" passes "$@" to TCC TROUBLESHOOTING §20, undefined symbol main hint CompilerDiagnostics.looksLikeMissingMain Output Panel for no main linker signature, CEntryWrapper compiles single C file no main exactly one function through generated wrapper and runs it TROUBLESHOOTING §22, first-hour tiles 33.1 three starter tiles C/Python/HTML DataStore first_launch_complete flag pure WelcomeStarters + WelcomeScreen idempotent create-or-open launch-state saved before flag flips, Packages hub Languages & IntelliSense open Unix tools collapsed search flattens) (→ `/start`, ch-07).
   - **Projects hub:** card list type mark ⌥ branch · N files · age change badge amber ↑N when commits never reached remote filter chips + search + ONE + sheet New Project / Clone Git Repo / Import ZIP / Open Folder + safe folder walk Phase 43.1 bounded Throwable-safe folder walk crash is StackOverflowError escaping unbounded copyDocumentChildren caught only as Exception then open-folder-as-project via ProjectLink + persisted SAF grant Phase 43.2 FileManager.projectDirCandidates lists filesDir/CodeC/projects + externalFilesDir/CodeC/projects noexec physics emulated storage mounted noexec so linked project runs from internal mirror sync in on open/save push-set on save-back excludes CodeC own outputs 39.1 must land first + export-all ZIP over both roots byte-identical + ZIP caps 10k entries 128 MB total-bytes cap path-escape check (→ ch-06).
   - **Honest git + GitHub truth:** staged per-file, conflicts grouped purple Mark Resolved block commit, failed push never looks successful "Committed locally ✓ — NOT pushed: …" PUSH retry, unpushed badge, Switch Branch branch list local+remote + New branch… Spck promise dirty work stashed and restored, plus Phase 40 GitReadiness pre-flight is git installed · token stored · is repo · has remote · branch has upstream · anything to push computed pure data rendered before user taps Install Git routed to Modules installer, clone error shows inside dialog not behind, PushOutcome/PushParser result card what pushed to which remote/branch Everything up-to-date * [new branch] or still local because X, Publish to GitHub via POST /user/repos private by default ls-remote-verified adopt-existing (→ ch-11).
   - **Web preview + LAN server:** loopback server over project folder relative CSS/JS fetch data.json ES modules work live reload on save console output shows under page, plus Phase 37 LAN opt-in 0.0.0.0 bind pool 8100–8199 never <1024 flask/fastapi/C templates honour CODEC_SERVER_HOST LanAddress pure + LanAddressProvider Android ServerEndpoints.of(port,bind,lanAddress,lanShared) single place two URLs refuses to advertise unreachable LAN URL panel states why ServerSharePanel both URLs with copy + ZXing core 3.5.4 QR Apache-2.0 assets/licenses/ZXING_APACHE2.txt About line + open in browser via OpenInBrowser/ShareActions copy fallback, ServerRegistry one owner port truth + ServerHost one owner socket/process leaving editor keeps LAN server serving while loopback preview dies with screen same RunForegroundService no second type Serving <project> on <ip>:<port> + Stop ticker suppressed (→ `/start`, ch-12, ch-14).
   - **Device APIs (CodeCApi):** battery/sensor/TTS/camera/intent — CodeC answer to Termux:API + clipboard/notify/toast/share/open-url/vibrate (Phase 5.3, 4.7, 4.8, 18) ops battery.status sensor.read tts.speak camera.capture intent.send markers NEED_PERMISSION: CAPTURING: camera output $PREFIX/tmp/codec-api/camera/ TTS 32 KiB cap permission flow (→ ch-13).
   - **Outputs temporary never in repo:** Phase 39 RunArtifacts routes every language's build output to filesDir/CodeC/temp/runs/<stamp>/ + TempGc age/capacity/newest-N prunes on start and after Stop RepoHygiene ~60 patterns incl .codec/ derived from CC0 github/gitignore templates plus CodeC own .codec/ .codec.json applied at single choke point stageAll git rm --cached already tracked what will be committed list before commit user's .gitignore always wins.
   - **Feedback & support:** Phase 41 hardcoded DeveloperContact WHATSAPP_E164 916296746606 display +91 62967 46606 EMAIL chakraborttypabi2772006@gmail.com single source every channel reads FeedbackScreen own screen Settings one OPEN row audit control 46 exit survey back-at-root Enjoying CodeC? 💚 star row / SHARE EXPERIENCE / GIVE A REVIEW / NOT NOW / EXIT tap back again to exit outside taps disabled rating rides report info line · Rating: 4/5 NOTHING uploaded by itself GIVE A REVIEW opens public repo whole prompt off-able feedback_exit_prompt_enabled default ON crash-log.txt header-first frame-capped COPY ALL yields complete record dialog title exception line versionName carries CI run number OpenInBrowser.openOrCopy shared policy no telemetry three honest disclosure lines ephemeral checkboxes (→ ch-15, ch-17, FAQ).
   - **Safe & shareable:** Phase 42 universal APK 6.6 MB -74% via R8+shrinkResources proguard-rules.pro law file okhttp removed per-ABI splits measured <2% and reverted assets/tcc not filtered, backup include-list-only FullBackupContent law projects only token/userland not backed up, crash-loop guard safe mode 3rd failed launch export+report hand-off, export-all ZIP over both roots byte-identical, 11 privacy rows audit-pinned telemetry scan, release channel app-v* tags only version guard UpdatePolicy name-grammar SHA256 lines RELEASE_NOTES template asserted by check_release_notes.sh, BETA.md known issues B-1…B-8, DATA_AND_PRIVACY.md no analytics no ads no account no crash upload only network requests you start bootstrap/package downloads you approve git against repo you configured update check when you tap it.

3. **The story** — built in public: phases 0 through 43 (0–19 core terminal+package+editor+projects+git+preview+CodeCApi, 20–24 multi-lang toolchain round 4 libllvm/clang+nodejs/npm+php+ruby+lua54 + language registry + tool probe + Auto engine, 25–33 editor quality + sora + ghost + strip + TextMate + snippets+Emmet + LSP + phone canvas + RUN chooser + first-hour tiles, 34–37 UX/UI file icons + typing feel + terminal speed + LAN server, 38–43 share-readiness identity + outputs temporary + GitHub truth + feedback + share-readiness + file-system strength) device-accepted and merged, from first compiler round to signed universal release; link `docs/JOURNEY.md` as full timeline. No ego, no metrics fluff, mention PR #71 merge commit 3c983c6 main CI 34602562874 Phase 42 closes.

4. **Engineering facts** (what makes it different, v2.2): original >_ mark (docs/icon/codec-mark.svg safe-zone-verified adaptive+monochrome+legacy PNGs + ic_stat_codec notification silhouette + app_mark About header + sharp 0.35.4 deterministic md5 + check_icon_assets.sh CI step), static musl TCC toolchain embedded APK arm64-v8a 3.7M + x86_64 3.6M + jniLibs libtcc.so 581600+405352 B + assets/textmate 2.3M, tccBinary() null on armeabi-v7a/x86 by design, signed package repository (never trusted=yes, gpgv verification, signed-by=, bootstrap userland-v2-dev SHA-256 verified staged atomic, validate-repository.py + validate-bootstrap.py pre-release gates), CI-built APK GitHub Actions Build APK runs assembleDebug + testDebugUnitTest + lintDebug inside assemble + Check icon assets step + measure lane throwaway key + release lane signed with upload key kept in Actions secrets KEYSTORE_PATH/STORE_PASSWORD/KEY_PASSWORD env my-upload-key.jks not in repo, targetSdk 28 deliberate keeps downloaded compilers executable why GitHub not Play (Play demands AAB API-36 targeting from 2026-08-31 privacy policy 12 opted-in closed-testers 14 days, Termux uses same compatibility mode), clean-room approach — features replicated from public behavior and specs code never copied law plainly (rule.md §6), R8 + shrinkResources law file every keep needs named proven failure first jdt.annotation dontwarn run 34577896124 okhttp pair removed on grep proof per-ABI splits built measured and reverted owner 0.94–1.77% vs 15% law assets/tcc/<abi> rides every split x86 kept measure-only release lane throwaway key uploads nothing, backup XMLs include-list-only after FullBackupContent lint law exclude-under-include hard error runs 34561139415/34567771307 tuition, crash-log.txt header-first frame-capped, FeedbackDraft wa.me hardcoded DeveloperContact no telemetry, etc.

5. **Links block:** Repo · README · JOURNEY · NEXT_STEPS · Releases · Issues · Package repo https://pabi277.github.io/CodeC/dev · BETA.md · DATA_AND_PRIVACY.md · RELEASE_NOTES.md template · UPLOAD_KEY_SETUP.md · TROUBLESHOOTING.md · icon source docs/icon/codec-512.png + codec-mark.svg + render_icon.mjs.

### Meta: title "About CodeC — >_ mark, 6.6 MB universal, Auto engine, feedback, backup, LAN server".

## 2. Implementation steps

1. Build the page (active nav: About, >_ mark favicon).
2. Source line per section recorded in chat-web3/; JOURNEY link points at file in repo stable URL; icon facts from chat-phase38 README + PART_38_1_APP_ICON.md, Settings trim from PART_38_2_SETTINGS_TRIM.md + SETTINGS_AUDIT.md, outputs temporary from PART_39, GitHub truth from PART_40, feedback from PART_41 + DeveloperContact.kt, share-readiness from PART_42 + RELEASE_NOTES.md + BETA.md, file-system strength from PART_43.
3. Self-dependent sweep (plan §5.5).

## 3. Exit condition

```text
1. Render clean at 360/1440; all 5 sections present in order v2.2 (what it is + >_ mark, feature tour with file icons + typing feel + ghost + strip + snippets+Emmet + TextMate + phone canvas + RUN chooser + first-hour tiles + safe walk + ProjectLink + export-all + GitHub truth + LAN server + CodeCApi + outputs temporary + feedback + safe & shareable, story phases 0–43, engineering facts with icon + TCC + signed repo + CI + targetSdk 28 + R8 + backup + crash-log + feedback + no telemetry, links block).
2. Every feature claim matches README + BETA + RELEASE_NOTES + TROUBLESHOOTING + JOURNEY (spot-check recorded in chat-web3/); icon facts match docs/icon + render_icon.mjs deterministic md5 + check_icon_assets.sh CI step; Settings trim 13→11 Termux card deleted but mechanism stays; outputs temporary RunArtifacts + RepoHygiene; GitHub truth GitReadiness + PushOutcome; feedback hardcoded +91 62967 46606 / email + exit survey + crash-log header-first; universal APK 6.6 MB -74% per-ABI reverted reason; backup include-list-only; crash-loop guard safe mode 3rd launch; export-all over both roots; safe walk planned Throwable-safe; ProjectLink persisted SAF grant noexec mirror.
3. Clean-room paragraph present and accurate (mirrors rule.md §6 wording in spirit) + no telemetry + privacy rows.
4. Sweep PASS. W3 total: product wing 7/7 pages → report + merge gate.
```
