# CodeC Phase 13 — GitHub Integration
Status: planned · cost [client-only] · depends Phase 8 + 11
Source refs: MainActivity.kt (Settings token), FileManagerScreen.kt, git CLI, docs/chat-phase8/
D1: Clone/commit/push screen; repo URL input; token stored app-private in Settings; clone → folder; commit message + push buttons; terminal fallback for git commands.
Exit device: paste URL → clone → files appear → edit → commit with message → push → success; token removable in Settings.
Not in scope: full GitHub marketplace / PR creation (can be done via terminal if needed), private repo access beyond token scope.
